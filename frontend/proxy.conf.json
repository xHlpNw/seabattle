{
  "/api": {
    "target": "http://localhost:8080",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "debug"
  }
}

// NOTE: To access from another computer on the network:
// 1. Find your computer's IP address (run: ipconfig on Windows, ifconfig on Linux/Mac)
// 2. Replace "localhost" above with your IP address (e.g., "http://192.168.1.100:8080")
// 3. Restart the Angular dev server
